apiVersion: keyline.keyline.dev/v1alpha1
kind: KeylineApplication
metadata:
  labels:
    app.kubernetes.io/name: keyline-operator
    app.kubernetes.io/managed-by: kustomize
  name: keylineapplication-sample
spec:
  # TODO(user): Add fields here

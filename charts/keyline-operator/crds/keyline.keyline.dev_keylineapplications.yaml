---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: keylineapplications.keyline.keyline.dev
spec:
  group: keyline.keyline.dev
  names:
    kind: KeylineApplication
    listKind: KeylineApplicationList
    plural: keylineapplications
    singular: keylineapplication
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KeylineApplication is the Schema for the keylineapplications
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of KeylineApplication
            properties:
              accessTokenHeaderType:
                description: AccessTokenHeaderType controls the JWT header type.
                enum:
                - at+jwt
                - JWT
                type: string
              claimsMappingScript:
                description: ClaimsMappingScript is an optional custom claims mapping
                  script.
                type: string
              deviceFlowEnabled:
                description: DeviceFlowEnabled enables the device authorization flow.
                type: boolean
              displayName:
                description: DisplayName is the human-readable label shown in the
                  Keyline UI.
                maxLength: 255
                minLength: 1
                type: string
              name:
                description: Name is the unique application identifier in Keyline.
                maxLength: 255
                minLength: 1
                type: string
              postLogoutUris:
                description: PostLogoutUris is the list of allowed post-logout redirect
                  URIs.
                items:
                  type: string
                type: array
              projectRef:
                description: ProjectRef references the KeylineProject this application
                  belongs to.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              redirectUris:
                description: RedirectUris is the list of allowed OAuth2 redirect URIs.
                items:
                  type: string
                minItems: 1
                type: array
              signingAlgorithm:
                description: SigningAlgorithm overrides the virtual server's signing
                  algorithm for tokens issued to this application.
                enum:
                - RS256
                - EdDSA
                type: string
              type:
                description: Type is the application type.
                enum:
                - public
                - confidential
                type: string
            required:
            - displayName
            - name
            - projectRef
            - redirectUris
            - type
            type: object
          status:
            description: status defines the observed state of KeylineApplication
            properties:
              applicationId:
                description: ApplicationId is the UUID of the application in Keyline.
                type: string
              conditions:
                description: conditions represent the current state of the KeylineApplication
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "keyline-operator.fullname" . }}-leader-election
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "keyline-operator.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "keyline-operator.fullname" . }}-leader-election
subjects:
- kind: ServiceAccount
  name: {{ include "keyline-operator.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
